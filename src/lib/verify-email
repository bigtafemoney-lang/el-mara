"use client";

import Link from "next/link";
import { useSearchParams } from "next/navigation";

export default function VerifyEmailPage() {
  const searchParams =
    useSearchParams();

  const email =
    searchParams.get("email");

  return (
    <main className="flex min-h-screen items-center justify-center bg-[#080808] px-6 text-[#f4f0e8]">
      <div className="w-full max-w-md text-center">

        <p className="text-[10px] uppercase tracking-[0.5em] text-[#c7a96b]">
          EL MARA
        </p>

        <h1 className="mt-6 text-4xl font-light">
          Vérifie ton email
        </h1>

        <p className="mt-6 text-sm leading-7 text-white/40">
          Nous avons envoyé un lien de
          vérification à :
        </p>

        {email && (
          <p className="mt-3 break-all text-sm text-[#c7a96b]">
            {email}
          </p>
        )}

        <p className="mt-8 text-xs leading-6 text-white/25">
          Ouvre ton email et clique sur le lien
          de vérification pour activer ton compte.
          Le lien expire après un certain temps.
        </p>

        <Link
          href="/login"
          className="mt-10 inline-flex border border-[#c7a96b]/60 px-8 py-4 text-[10px] uppercase tracking-[0.3em] transition-all hover:bg-[#c7a96b] hover:text-black"
        >
          Aller à la connexion
        </Link>
      </div>
    </main>
  );
}